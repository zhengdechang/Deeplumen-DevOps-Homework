server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Let the ingress handle /api. Everything else is served as the frontend application.
    location / {
        try_files $uri $uri/ /index.html;
    }
}